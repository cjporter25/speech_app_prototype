Establish Environment:
    Go to:
    https://nodejs.org/
    Download the LTS version (the one labeled “Recommended for most users”).
    Install it using default settings (npm will be included).
    Restart your command prompt (or open a new one), then run:


Create Virtual Environment
python -m venv .venv

Activate Virtual Environment
.venv\Scripts\activate

Upgrade Pip in current Directory
    .\.venv\Scripts\python.exe -m pip install --upgrade pip 


Setting up Git in Visual Studio
- Sign into GitHub in VS Code
- Initialize repository
- Create first commit with message
- THEN cick "publish"